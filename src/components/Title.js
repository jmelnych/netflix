import React from 'react';

class Title extends React.Component {
    render() {
        const homeworkCount = 1;

        return (
            <h1>Homework #{homeworkCount}</h1>
        )
    }
}

export default Title;